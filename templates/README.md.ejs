# <%= title %> (Ionic + AngularFire)

Generated by **@oneassembly/ionic-angular-init**.

## Environments
- Dev: `src/environments/environment.ts`
- Prod: `src/environments/environment.prod.ts`
- Staging (dev build): `src/environments/environment.staging.ts`
- Staging (prod build): `src/environments/environment.prod.staging.ts`
- Examples: `src/environments/environment.example.ts`, `environment.prod.example.ts`

## Scripts
- `npm start` — dev server
- `npm run start:staging` — serve with staging config
- `npm run build` — prod build
- `npm run build:staging` — staging build (non-prod optimizations)
- `npm run build:prod:staging` — prod-optimized build pointing at staging
- `npm run deploy:firebase:prod` / `:staging`

## Firebase
Add your keys to the environment files, then deploy with Firebase Hosting.

<% if (firebase) { %>
## Firebase Configuration
This project is configured with Firebase hosting. See the generated `firebase.json` and `.firebaserc` files for deployment configuration.

<% if (ci === 'github') { %>
## CI/CD
GitHub Actions workflow is configured to automatically deploy to Firebase Hosting on push to:
- `<%= branchProd %>` branch → Production
- `<%= branchStaging %>` branch → Staging

Make sure to set the `FIREBASE_SERVICE_ACCOUNT` secret in your GitHub repository settings.
<% } %>
<% } %>

## Getting Started
See the `docs/` folder for detailed setup and deployment instructions.
