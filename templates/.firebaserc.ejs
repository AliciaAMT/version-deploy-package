{
  "targets": {
    "REPLACE_WITH_PROJECT_ID": {
      "hosting": {
        "prod": ["<%= hostingSites.prod || 'REPLACE_WITH_PROD_SITE' %>"],
        "staging": ["<%= hostingSites.staging || 'REPLACE_WITH_STAGING_SITE' %>"]
      }
    }
  }
}
